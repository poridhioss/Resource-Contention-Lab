apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-service
  namespace: contention-lab
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-service
  template:
    metadata:
      labels:
        app: api-service
        version: priority
    spec:
      priorityClassName: high-priority-api  # High priority scheduling
      containers:
        - name: api
          image: contention-lab/api-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8000
          resources:
            requests:
              cpu: "300m"
              memory: "128Mi"
            limits:
              cpu: "800m"
              memory: "256Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 5
